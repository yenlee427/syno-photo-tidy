# syno-photo-tidy
Windows photo organizer that isolates thumbnails, keeps only the highest-resolution duplicate, moves (never deletes) files to a TO_DELETE folder, supports Synology-style renaming, year/month archiving, dry-run preview, and full rollback via manifest.
